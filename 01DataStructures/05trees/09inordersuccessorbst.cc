#include <iostream>
#include <queue>

// graph representation!

// Use a vertex list for vertex!
// AUse a edge list for edges (two endpoints and weight)
class Edge{
    // unidirected graph startnode interchangable with endnode
    // directed graph order is important!
    public:
        char *startNode;
        char *endNode;
};

int main(){
    return 0;
}