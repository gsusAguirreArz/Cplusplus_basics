#include <iostream> // header, liibraries for your program (iostream console output input)
using namespace std; // avoid uing std on each line

int main(){
    cout << "Hello World!\n"; // this will output to the console hello world
    cout << "My name is Jesus" << endl;
    cout << "Today is Monday";
    // system("pause>0");
    return 0;
}

// to compile in ubuntu type g++ -o OUTPUT_FILENAME FILE_TO_COMPILE
// tu run it just ./OUTPUT_FILENAME